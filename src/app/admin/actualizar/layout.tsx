import { IChildren } from '@/interfaces/iChildren'
import React from 'react'

export default function Actualizarlayout({children}: IChildren){
  return (
    <main>
        { children }
    </main>
  )
}